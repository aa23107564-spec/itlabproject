# KeyboardIntroPage 組件

## 📋 概述

`KeyboardIntroPage` 是一個通用的引導頁面組件，用於在每個章節開始前顯示鍵盤圖標提示。用戶按下任意鍵後可跳過引導頁面，進入章節內容。

## 🎯 功能特點

- ✅ 白色背景的全屏引導頁面
- ✅ 居中顯示鍵盤圖標
- ✅ 顯示操作說明文字（選字、確定、按下任意鍵開始）
- ✅ 使用像素字體（PixelFont）
- ✅ 響應任意鍵盤按鍵
- ✅ 平滑的淡入動畫效果
- ✅ 鍵盤圖標和文字浮動動畫
- ✅ 文字動畫延遲效果
- ✅ 響應式設計
- ✅ 自動清理事件監聽器

## 📦 使用方法

### 基本用法

```jsx
import React, { useState } from 'react';
import KeyboardIntroPage from './KeyboardIntroPage';

function MyChapter() {
  const [showIntro, setShowIntro] = useState(true);

  const handleSkipIntro = () => {
    setShowIntro(false);
  };

  return (
    <>
      {/* 引導頁面 */}
      {showIntro && <KeyboardIntroPage onSkip={handleSkipIntro} />}
      
      {/* 章節內容 */}
      <div>
        章節內容...
      </div>
    </>
  );
}
```

## 🔧 Props

| 屬性 | 類型 | 必需 | 說明 |
|-----|------|------|------|
| `onSkip` | `function` | 是 | 按下任意鍵時的回調函數 |

## 🎨 樣式

### CSS 類名

- `.keyboard-intro-page` - 引導頁面容器
- `.keyboard-intro-content` - 內容容器
- `.keyboard-icon` - 鍵盤圖標
- `.keyboard-text-container` - 文字容器
- `.keyboard-text-line` - 文字行
- `.keyboard-text` - 文字元素

### 動畫效果

1. **淡入動畫** (`fadeIn`)
   - 持續時間：0.5秒
   - 效果：從透明到不透明

2. **圖標浮動動畫** (`floatAnimation`)
   - 持續時間：3秒
   - 效果：上下浮動 10px
   - 循環：無限循環

3. **文字浮動動畫** (`textFloatAnimation`)
   - 持續時間：2.5秒
   - 效果：上下浮動 5px
   - 循環：無限循環
   - 延遲：不同文字有不同延遲（0s, 0.3s, 0.6s）

4. **淡出動畫** (`fadeOut`)
   - 持續時間：0.3秒
   - 效果：從不透明到透明

### 文字樣式

- **字體**：PixelFont（像素字體）
- **大小**：18px（主要文字）、16px（說明文字）
- **顏色**：#333333（主要文字）、#666666（說明文字）
- **陰影**：白色描邊效果
- **動畫**：上下浮動 + 延遲效果

### 自定義樣式

可以通過修改 `src/styles/keyboardIntro.css` 來自定義樣式：

```css
/* 修改背景顏色 */
.keyboard-intro-page {
  background-color: #f5f5f5;
}

/* 修改圖標大小 */
.keyboard-icon {
  max-width: 60vw;
  max-height: 60vh;
}

/* 修改動畫速度 */
.keyboard-icon {
  animation: floatAnimation 2s ease-in-out infinite;
}
```

## 📱 響應式設計

- 圖標自動適應螢幕大小
- 最大寬度：80vw
- 最大高度：80vh
- 保持圖片比例（`object-fit: contain`）

## 🔑 支持的按鍵

組件響應**所有鍵盤按鍵**，包括但不限於：

- 字母鍵（A-Z）
- 數字鍵（0-9）
- 方向鍵（↑ ↓ ← →）
- 功能鍵（Enter、Space、Escape 等）
- 特殊鍵（Shift、Ctrl、Alt 等）

## 📝 文字內容

組件顯示以下文字內容：

1. **第一行**：`選字` `確定`
2. **第二行**：`按下任意鍵開始`

文字使用像素字體，並具有不同的動畫延遲效果，創造出波浪般的浮動動畫。

## 🎬 使用場景

### 章節一

```jsx
// src/components/Chapter1.js
import KeyboardIntroPage from './KeyboardIntroPage';

function Chapter1() {
  const [showIntro, setShowIntro] = useState(true);
  
  return (
    <>
      {showIntro && <KeyboardIntroPage onSkip={() => setShowIntro(false)} />}
      {/* 章節一內容 */}
    </>
  );
}
```

### 章節二

```jsx
// src/components/Chapter2.js
import KeyboardIntroPage from './KeyboardIntroPage';

function Chapter2() {
  const [showIntro, setShowIntro] = useState(true);
  
  return (
    <>
      {showIntro && <KeyboardIntroPage onSkip={() => setShowIntro(false)} />}
      {/* 章節二內容（視覺小說） */}
    </>
  );
}
```

### 章節三

```jsx
// src/components/Chapter3.js
import KeyboardIntroPage from './KeyboardIntroPage';

function Chapter3() {
  const [showIntro, setShowIntro] = useState(true);
  
  return (
    <>
      {showIntro && <KeyboardIntroPage onSkip={() => setShowIntro(false)} />}
      {/* 章節三內容（文字遊戲） */}
    </>
  );
}
```

## 🧪 測試

運行測試：

```bash
npm test KeyboardIntroPage.test.js
```

測試覆蓋：

- ✅ 組件渲染
- ✅ 鍵盤事件響應
- ✅ 回調函數調用
- ✅ 事件監聽器清理
- ✅ 樣式類名驗證

## 🔄 生命週期

1. **組件掛載**
   - 添加鍵盤事件監聽器
   - 播放淡入動畫
   - 開始浮動動畫

2. **用戶按鍵**
   - 觸發 `onSkip` 回調
   - 父組件隱藏引導頁面

3. **組件卸載**
   - 移除鍵盤事件監聽器
   - 清理動畫資源

## 🎯 最佳實踐

1. **使用 useState 控制顯示**
   ```jsx
   const [showIntro, setShowIntro] = useState(true);
   ```

2. **提供明確的回調函數**
   ```jsx
   const handleSkipIntro = () => {
     setShowIntro(false);
     // 可選：添加其他邏輯
   };
   ```

3. **確保圖片資源存在**
   ```
   public/images/icons/鍵盤.png
   ```

4. **保持 z-index 層級**
   ```css
   .keyboard-intro-page {
     z-index: 10000; /* 確保在最上層 */
   }
   ```

## 📂 相關文件

- `src/components/KeyboardIntroPage.jsx` - 組件實現
- `src/styles/keyboardIntro.css` - 組件樣式
- `src/components/__tests__/KeyboardIntroPage.test.js` - 單元測試
- `public/images/icons/鍵盤.png` - 鍵盤圖標

## 🐛 故障排除

### 問題：圖標不顯示

**解決方案：**
1. 檢查圖片路徑是否正確
2. 確認圖片文件存在於 `public/images/icons/` 目錄
3. 檢查 `process.env.PUBLIC_URL` 配置

### 問題：按鍵無反應

**解決方案：**
1. 檢查 `onSkip` 回調是否正確傳遞
2. 確認父組件的狀態更新邏輯
3. 檢查瀏覽器控制台是否有錯誤

### 問題：頁面不會消失

**解決方案：**
1. 確保父組件正確處理 `showIntro` 狀態
2. 檢查條件渲染邏輯：`{showIntro && <KeyboardIntroPage />}`
3. 驗證 `onSkip` 函數是否更新狀態

## 🚀 未來改進

- [ ] 添加淡出動畫效果
- [ ] 支持自定義圖標
- [ ] 添加文字提示選項
- [ ] 支持觸摸設備
- [ ] 添加音效支持
- [ ] 支持多語言提示

